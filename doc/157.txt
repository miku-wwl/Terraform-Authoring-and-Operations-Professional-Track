Hi everyone, and welcome to the overview video associated with challenge number two. So let's get started. Now the main aim of this specific challenge is to test your understanding about modules. Modules again is a very important topic. And specifically when we discuss about module the main focus would be towards the module refactoring related use cases. So along with that as part of the refactoring, also there are some other areas that you will be tested against like data sources. So as a Terraform professional again you need to be very familiar with modules and module refactoring related use cases. So refactoring is going to be the major part of this challenge. Now when you explore the challenge number two let's open this. In total, this challenge is divided into a total of seven tasks. Again, as we were discussing, each task has the appropriate description on the steps that you need to take. So as part of this challenge, you will be given a monolithic configuration. Basically, it will contain a single main.tf and you have to refactor it into multiple child modules. So you have to split it into multiple child modules. And along with that, there are a few instances, like the data sources that you have to configure as part of the resources. So again this is a minor part, but the major part that you need to solve in order to complete the challenge would be related to the module refactoring use case. So that's an overview. And with this we'll conclude today's video.