# 第 163 节：挑战四概览：count、for 表达式与输出

## 知识点讲解

本节对应 `doc/163.txt` 的课程录音转写。综合挑战系列的重点不是记住某一个资源参数，而是在限定时间内把题目拆成可执行步骤：读题、定位资源、修正配置、应用、输出、验证、销毁。

本项目把课程里的真实 AWS 操作改造成 LocalStack 或本地 provider 可验证的练习。所有 AWS API 默认指向 `http://localhost:4566`，凭证固定为 `test/test`，不使用真实 AWS 账号。

## 实验目标

- 理解 count、for expression 和输出结构题型。
- 识别 CSV 输入到结构化输出的转换路径。
- 为 164~165 和 170 做准备。

## 实验边界

- 不访问真实 AWS 账号。
- 不写入真实 access key、secret key 或 root 账号信息。
- 所有文档和题面使用中文。
- 本节是挑战说明或备考方法，不创建真实 AWS 资源。

## 参考实现

参考实现位于 `practice/labs/163/`。该目录是闭环答案，用于验证题目设计，不是上机做题目录。

## 验证结论

- 分类：`aws-theory`
- 接受状态：accept
- 验证方式：`terraform fmt`、`terraform init`、`terraform validate`、`terraform plan`、`terraform apply`、验证脚本、`terraform destroy`；理论型 lab 使用脚本验证 checklist。
