# Terraform 实操训练 165：挑战四解法二：EC2 配置输出结构化（讲解版）

## 1. 本节核心：不要只输出一个值，要输出“结构化配置结果”

这一节是综合挑战四的解法二，主题是：

> EC2 配置输出结构化。

它不是单纯考你会不会创建 EC2，而是考你能不能把 EC2 的关键配置用 Terraform output 清楚、稳定、可验证地暴露出来。

普通 output 可能是这样：

```hcl
output "instance_id" {
  value = aws_instance.app.id
}
```

这只能输出一个值。

结构化 output 更像这样：

```hcl
output "ec2_config" {
  value = {
    instance_id   = aws_instance.app.id
    instance_type = aws_instance.app.instance_type
    ami_id        = aws_instance.app.ami
    tags          = aws_instance.app.tags
  }
}
```

这样输出的是一个对象。  
它能表达“这台 EC2 的配置结果”，而不是零散几个字符串。

---

## 2. 为什么挑战题要练结构化 output？

综合挑战题经常会有验证脚本。

验证脚本不只是看 Terraform 有没有 apply 成功，还会检查：

```text
output 是否存在
output 名称是否正确
output 类型是否正确
output 字段是否完整
output 值是否符合预期
```

如果你只输出一个字符串，验证脚本很难判断完整配置。

结构化 output 的价值是：

```text
1. 信息更完整
2. 字段更稳定
3. 便于脚本验证
4. 便于下游模块或 workspace 读取
5. 便于人类排查
6. 更接近生产环境接口设计
```

Terraform output 不是随便打印一下，它经常是模块、workspace、CI/CD、验证脚本之间的接口。

---

## 3. 什么叫结构化 output？

结构化 output 指的是输出一个有组织的数据结构，而不是一堆散乱值。

常见结构包括：

```text
object
map
list
list(object)
map(object)
```

例如 object：

```hcl
output "ec2_config" {
  value = {
    id            = aws_instance.app.id
    ami           = aws_instance.app.ami
    instance_type = aws_instance.app.instance_type
  }
}
```

例如 list：

```hcl
output "instance_ids" {
  value = [
    aws_instance.app1.id,
    aws_instance.app2.id
  ]
}
```

例如 map：

```hcl
output "instance_map" {
  value = {
    app1 = aws_instance.app1.id
    app2 = aws_instance.app2.id
  }
}
```

例如 map(object)：

```hcl
output "instances" {
  value = {
    app = {
      id            = aws_instance.app.id
      instance_type = aws_instance.app.instance_type
      ami           = aws_instance.app.ami
    }
  }
}
```

结构越清楚，下游越好用。

---

## 4. EC2 配置通常应该输出哪些字段？

结构化输出不是越多越好，而是输出“有意义、可验证、可复用”的字段。

EC2 常见输出字段包括：

```text
instance_id
ami_id
instance_type
availability_zone
private_ip
public_ip
subnet_id
security_group_ids
iam_instance_profile
tags
```

在 LocalStack 实验里，不一定所有字段都真实有意义。  
但结构设计仍然值得训练。

一个比较合理的基础输出：

```hcl
output "ec2_config" {
  value = {
    instance_id        = aws_instance.app.id
    ami_id             = aws_instance.app.ami
    instance_type      = aws_instance.app.instance_type
    subnet_id          = aws_instance.app.subnet_id
    security_group_ids = aws_instance.app.vpc_security_group_ids
    tags               = aws_instance.app.tags
  }
}
```

这比只输出 `instance_id` 更有表达力。

---

## 5. 结构化 output 的核心好处：稳定接口

如果你输出一堆零散字段：

```hcl
output "instance_id" {}
output "instance_type" {}
output "ami_id" {}
output "subnet_id" {}
output "tags" {}
```

字段多了以后会很散。

结构化后：

```hcl
output "ec2_config" {
  value = {
    instance_id   = ...
    instance_type = ...
    ami_id        = ...
    subnet_id     = ...
    tags          = ...
  }
}
```

下游只要读取一个 output：

```text
ec2_config
```

然后从里面取字段：

```text
ec2_config.instance_id
ec2_config.instance_type
ec2_config.tags
```

这就像 API 返回 JSON 对象一样。

Terraform output 也应该有接口设计意识。

---

## 6. output 是对外契约，不是随便起名

挑战题里尤其要注意 output 名称。

如果题目要求：

```hcl
output "ec2_config"
```

你就不要改成：

```hcl
output "instance_config"
```

或者：

```hcl
output "server_info"
```

从你个人角度看也许更顺眼，但验证脚本可能直接失败。

真实项目也一样。  
如果下游 workspace 通过 remote state 读取：

```hcl
data.terraform_remote_state.compute.outputs.ec2_config
```

你把 output 改名，下游就炸。

所以记住：

```text
output 名称是接口，不是装饰。
```

---

## 7. 结构化 output 和验证脚本的关系

验证脚本可能会做类似检查：

```sh
terraform output -json ec2_config
```

然后解析 JSON：

```json
{
  "instance_id": "i-xxxx",
  "ami_id": "ami-xxxx",
  "instance_type": "t3.micro",
  "tags": {
    "Name": "demo"
  }
}
```

如果 output 是结构化的，脚本可以精准判断：

```text
instance_type 是否等于预期
ami_id 是否存在
tags.Name 是否正确
security_group_ids 是否是列表
```

如果 output 只是人类可读字符串：

```hcl
output "ec2_summary" {
  value = "instance is t3.micro"
}
```

脚本就很难稳定解析。

所以工程实践里，给机器读的输出应该尽量结构化。

---

## 8. output 类型要稳定

Terraform 本身不强制你给 output 写 type，但你心里要有类型意识。

例如：

```hcl
output "security_group_ids" {
  value = aws_instance.app.vpc_security_group_ids
}
```

这通常是 list 或 set 类型。

不要把它拼成字符串：

```hcl
output "security_group_ids" {
  value = join(",", aws_instance.app.vpc_security_group_ids)
}
```

除非题目明确要求字符串。

结构化输出应尽量保留原始类型：

```text
字符串就输出 string
列表就输出 list
映射就输出 map
对象就输出 object
```

这样下游更好处理。

---

## 9. EC2 单实例结构化输出示例

如果只有一台 EC2，可以输出 object：

```hcl
output "ec2_config" {
  value = {
    instance_id        = aws_instance.app.id
    ami_id             = aws_instance.app.ami
    instance_type      = aws_instance.app.instance_type
    subnet_id          = aws_instance.app.subnet_id
    security_group_ids = aws_instance.app.vpc_security_group_ids
    private_ip         = aws_instance.app.private_ip
    tags               = aws_instance.app.tags
  }
}
```

这类 output 的好处是：

```text
字段清楚
层级简单
适合验证脚本
适合下游读取
```

---

## 10. EC2 多实例结构化输出示例：for_each 场景

如果 EC2 用 `for_each` 创建：

```hcl
resource "aws_instance" "app" {
  for_each = var.instances

  ami           = each.value.ami_id
  instance_type = each.value.instance_type

  tags = {
    Name = each.key
  }
}
```

结构化 output 可以这样写：

```hcl
output "ec2_configs" {
  value = {
    for name, instance in aws_instance.app : name => {
      instance_id   = instance.id
      ami_id        = instance.ami
      instance_type = instance.instance_type
      tags          = instance.tags
    }
  }
}
```

输出结果类似：

```json
{
  "web": {
    "instance_id": "i-web",
    "ami_id": "ami-xxx",
    "instance_type": "t3.micro",
    "tags": {
      "Name": "web"
    }
  },
  "worker": {
    "instance_id": "i-worker",
    "ami_id": "ami-yyy",
    "instance_type": "t3.small",
    "tags": {
      "Name": "worker"
    }
  }
}
```

这就是 `map(object)`。

它比 list 更稳定，因为 key 是业务身份。

---

## 11. 为什么多实例更推荐 map(object)？

如果你输出 list：

```hcl
output "ec2_configs" {
  value = [
    for instance in aws_instance.app : {
      id   = instance.id
      type = instance.instance_type
    }
  ]
}
```

问题是下游可能要依赖顺序：

```text
第 0 个是 web？
第 1 个是 worker？
```

这不稳。

如果用 map：

```hcl
output "ec2_configs" {
  value = {
    for name, instance in aws_instance.app : name => {
      id   = instance.id
      type = instance.instance_type
    }
  }
}
```

下游可以按 key 取：

```text
ec2_configs.web
ec2_configs.worker
```

这更清楚。

一句话：

```text
有业务身份的集合，优先 map(object)，少靠 list 下标猜。
```

---

## 12. 结构化 output 不等于输出所有东西

有些人一听结构化，就开始把整个 resource 都输出：

```hcl
output "ec2_config" {
  value = aws_instance.app
}
```

不推荐。

原因：

```text
1. 输出太大
2. 包含很多无关字段
3. 可能包含敏感或不稳定属性
4. Provider 升级后字段可能变化
5. 下游依赖内部细节，耦合变重
```

更好的方式是只输出需要的字段：

```hcl
output "ec2_config" {
  value = {
    instance_id   = aws_instance.app.id
    instance_type = aws_instance.app.instance_type
    ami_id        = aws_instance.app.ami
    tags          = aws_instance.app.tags
  }
}
```

这叫“稳定接口”。

---

## 13. sensitive output 怎么处理？

如果 output 包含敏感值，要标记：

```hcl
output "admin_password" {
  value     = random_password.admin.result
  sensitive = true
}
```

EC2 配置输出一般不应该包含：

```text
private key
password
token
secret
access key
user_data 中的密钥
```

如果真的要输出，也要非常谨慎。

本节挑战主题是 EC2 配置结构化，不是把所有内部细节暴露出去。  
结构化不是裸奔。

---

## 14. LocalStack 在本节里的作用

本项目把真实 AWS 操作改造成 LocalStack 或本地 provider 可验证的练习。

默认：

```text
AWS API endpoint = http://localhost:4566
credentials = test/test
```

这意味着：

```text
不访问真实 AWS
不使用真实 access key
不会产生 AWS 费用
不会启动真实 EC2
可以反复练习 plan/apply/verify/destroy
```

但边界也很明确：

```text
LocalStack 可以验证 Terraform 配置、状态变化、输出结构和 provider 调用路径
不能替代真实 AWS 的 IAM 权限、真实 EC2 启动、网络连通性、配额和实例运行状态验证
```

所以本节重点是 Terraform 工程能力，不是真实 EC2 运维能力。

---

## 15. 挑战题通用流程

本节属于综合挑战，所以不要只记 output 写法。  
你还要按挑战题流程做：

```text
1. 读题，确认要求输出什么结构
2. 定位 EC2 resource 或 module
3. 确认字段来源
4. 编写结构化 output
5. terraform fmt
6. terraform init
7. terraform validate
8. terraform plan
9. terraform apply
10. terraform output -json
11. 运行验证脚本
12. terraform destroy
```

尤其要多用：

```sh
terraform output
terraform output -json
```

结构化 output 最好用 JSON 看，能更直观看到类型和字段。

---

## 16. 实验运行手册

进入实验目录：

```sh
cd /workspace/practice/labs/165
```

格式化检查：

```sh
terraform fmt -check
```

初始化：

```sh
terraform init -input=false
```

配置校验：

```sh
terraform validate
```

查看计划：

```sh
terraform plan -input=false -no-color -out=tfplan
```

应用：

```sh
terraform apply -auto-approve tfplan
```

查看输出：

```sh
terraform output
terraform output -json
```

如果有验证脚本：

```sh
./verify.sh
```

销毁：

```sh
terraform destroy -auto-approve
```

清理：

```sh
rm -f tfplan
rm -rf .terraform .terraform.lock.hcl terraform.tfstate terraform.tfstate.backup
```

---

## 17. 运行时应该观察什么？

本节重点观察 output，不只是资源。

你要看：

```text
1. output 名称是否符合题目要求
2. output 是否是 object / map / list 等结构化类型
3. 字段名是否稳定
4. 字段值是否来自正确资源属性
5. terraform output -json 是否能被机器解析
6. 验证脚本是否能通过
7. destroy 后环境是否能清理
```

不要只满足于：

```text
terraform apply 成功
```

综合挑战题里，apply 成功但 output 错，仍然是失败。

---

## 18. 常见错误和排障

## 18.1 output 名称写错

题目要求：

```hcl
output "ec2_config"
```

你写：

```hcl
output "ec2"
```

验证脚本可能直接失败。

---

## 18.2 字段名写错

题目或验证脚本期望：

```text
instance_type
```

你写成：

```text
type
```

也可能失败。

结构化 output 的字段名也是接口的一部分。

---

## 18.3 输出值引用错资源

比如实际资源是：

```hcl
resource "aws_instance" "app" {}
```

你却写：

```hcl
value = aws_instance.web.id
```

会 validate 失败。

如果资源在 module 里，要通过 module output 转接：

```hcl
value = module.compute.ec2_config
```

---

## 18.4 输出类型不符合预期

验证脚本期望 list：

```json
"security_group_ids": ["sg-1", "sg-2"]
```

你输出 string：

```json
"security_group_ids": "sg-1,sg-2"
```

这就是类型不匹配。

尽量保留 Terraform 原始类型，不要随便 `join`。

---

## 18.5 输出整个 resource 对象

不要为了省事写：

```hcl
output "ec2_config" {
  value = aws_instance.app
}
```

这会暴露太多 provider 内部字段，也会让输出结构不稳定。

应该只挑选必要字段。

---

## 19. 生产环境推荐做法

真实项目里，模块 output 应该像 API 一样设计。

例如 compute module 输出：

```hcl
output "instance" {
  value = {
    id              = aws_instance.app.id
    arn             = aws_instance.app.arn
    private_ip      = aws_instance.app.private_ip
    subnet_id       = aws_instance.app.subnet_id
    security_groups = aws_instance.app.vpc_security_group_ids
    tags            = aws_instance.app.tags
  }
}
```

如果是多实例：

```hcl
output "instances" {
  value = {
    for name, instance in aws_instance.app : name => {
      id         = instance.id
      private_ip = instance.private_ip
      subnet_id  = instance.subnet_id
      tags       = instance.tags
    }
  }
}
```

设计原则：

```text
输出必要字段
字段名稳定
类型稳定
避免敏感信息
避免输出整个 resource
避免下游依赖 provider 内部细节
```

---

## 20. output 和 remote state 的关系

结构化 output 在 HCP Terraform / remote state 场景里非常有用。

下游 workspace 可以读取：

```hcl
data "terraform_remote_state" "compute" {
  backend = "remote"

  config = {
    organization = "example-org"
    workspaces = {
      name = "compute"
    }
  }
}
```

然后：

```hcl
locals {
  instance_id = data.terraform_remote_state.compute.outputs.ec2_config.instance_id
}
```

如果你的 output 结构稳定，下游就很好用。

如果你今天输出 string，明天改成 object，后天下游字段改名，下游 workspace 就会被你折腾坏。

所以 output 是跨 workspace 的合同。

---

## 21. 结构化 output 的判断标准

一个好的结构化 output 应该满足：

```text
1. 名称清楚
2. 字段清楚
3. 类型稳定
4. 不暴露敏感信息
5. 不暴露过多内部细节
6. 能被 terraform output -json 稳定解析
7. 下游读取时不需要猜
8. 验证脚本容易检查
```

差的 output 通常是：

```text
字段乱
类型乱
名称乱
一会儿 string 一会儿 object
靠字符串拼接表达复杂信息
把整个 resource 直接丢出去
```

挑战题就是逼你养成好习惯。

---

## 22. 考试 / 实战重点怎么记？

本节重点：

```text
EC2 配置可以通过结构化 output 暴露。
结构化 output 可以是 object、map、list、map(object)。
单实例适合 object。
多实例 for_each 适合 map(object)。
output 名称、字段名、类型都是接口。
验证脚本通常更容易检查 terraform output -json。
不要输出整个 resource，也不要输出敏感信息。
```

如果题目说：

> 需要输出 EC2 的实例 ID、AMI、实例类型和标签，供验证脚本读取。

答案通常不是拼字符串，而是：

```hcl
output "ec2_config" {
  value = {
    instance_id   = aws_instance.app.id
    ami_id        = aws_instance.app.ami
    instance_type = aws_instance.app.instance_type
    tags          = aws_instance.app.tags
  }
}
```

---

## 23. 快速自测

### 题目 1

为什么结构化 output 比拼接字符串更适合验证脚本？

答案：

```text
因为结构化 output 可以通过 terraform output -json 被稳定解析，脚本能直接检查字段名、字段类型和值。
```

---

### 题目 2

单台 EC2 的配置输出适合用什么结构？

答案：

```text
object。
```

例如：

```hcl
output "ec2_config" {
  value = {
    instance_id   = aws_instance.app.id
    instance_type = aws_instance.app.instance_type
    ami_id        = aws_instance.app.ami
  }
}
```

---

### 题目 3

多台 EC2 使用 `for_each` 时，为什么适合输出 `map(object)`？

答案：

```text
因为每台实例可以用稳定 key 表示业务身份，下游可以按 key 读取，不需要依赖 list 下标顺序。
```

---

### 题目 4

可以直接输出整个 `aws_instance.app` 吗？

答案：

```text
不推荐。这样会暴露过多 provider 内部字段，输出结构不稳定，也可能包含不该暴露的信息。
```

---

### 题目 5

LocalStack 通过是否说明真实 EC2 一定能启动成功？

答案：

```text
不说明。LocalStack 主要验证 Terraform 配置和输出结构，真实 AWS 还要验证 IAM、AMI、网络、安全组、配额和实例运行状态。
```

---

## 24. 记忆口诀

```text
单机用 object，
多机用 map。
字段要稳定，
类型别乱改。
脚本读 JSON，
别拼字符串。
```

再短一点：

```text
output 是接口，不是 print。
```

---

## 25. 最终总结

本节的核心是：

> 把 EC2 的关键配置通过结构化 Terraform output 暴露出来，让人和验证脚本都能稳定读取。

完整主线：

```text
定位 EC2 配置
-> 选择需要暴露的字段
-> 设计 object / map(object) output
-> terraform output -json 检查结构
-> verify 脚本验证
-> destroy 清理
```

一句话收尾：

```text
EC2 创建出来只是第一步；能把它的关键配置用稳定结构输出出来，才是可验证、可复用、可交接的 Terraform 工程能力。
```
