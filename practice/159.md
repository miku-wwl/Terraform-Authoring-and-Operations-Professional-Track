# 第 159 节：挑战二解法二：模块重构后的输出与状态映射

## 知识点讲解

本节对应 `doc/159.txt` 的课程录音转写。综合挑战系列的重点不是记住某一个资源参数，而是在限定时间内把题目拆成可执行步骤：读题、定位资源、修正配置、应用、输出、验证、销毁。

本项目把课程里的真实 AWS 操作改造成 LocalStack 或本地 provider 可验证的练习。所有 AWS API 默认指向 `http://localhost:4566`，凭证固定为 `test/test`，不使用真实 AWS 账号。

## 实验目标

- 完成 `挑战二解法二：模块重构后的输出与状态映射` 对应的 Terraform 练习。
- 在 LocalStack 或本地 provider 中完成 plan/apply/verify/destroy。
- 理解本节与真实 AWS 行为之间的边界。

## 实验边界

- 不访问真实 AWS 账号。
- 不写入真实 access key、secret key 或 root 账号信息。
- 所有文档和题面使用中文。
- 本节可在 LocalStack 中完成核心 Terraform 路径闭环。

## 参考实现

参考实现位于 `practice/labs/159/`。该目录是闭环答案，用于验证题目设计，不是上机做题目录。

## 验证结论

- 分类：`aws-localstack-pass`
- 接受状态：accept
- 验证方式：`terraform fmt`、`terraform init`、`terraform validate`、`terraform plan`、`terraform apply`、验证脚本、`terraform destroy`；理论型 lab 使用脚本验证 checklist。
