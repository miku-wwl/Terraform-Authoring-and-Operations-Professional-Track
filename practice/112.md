# 第 112 节：AWS Provider Assume Role 概念

## 知识点讲解

本节来自 doc/112.txt。核心主题是：使用 AWS provider 的 assume_role 块通过 STS 获取临时凭证，再创建资源。

本实验不访问真实 AWS。所有 AWS API 都通过 LocalStack 的 http://localhost:4566 endpoint 模拟，使用假凭证 	est/test。

## 实验目标

1. 理解本节课程中的 Terraform / AWS Provider 行为。
2. 能在 LocalStack 中完成初始化、计划、应用、验证、销毁。
3. 能区分课程中的真实 AWS 场景和本地模拟环境的边界。
4. 能把本节能力迁移到真实项目中。

## 参考实现

参考实现位于 practice/labs/112/。该目录是闭环答案，用于验证题目设计，不是上机做题目录。

## 验证结论

- 分类：aws-localstack-limited
- 接受状态：accept
- 备注：LocalStack 可验证 provider assume_role 配置和 STS 调用路径；真实 AWS 的跨账号信任策略仍需在真实账号中验证。
