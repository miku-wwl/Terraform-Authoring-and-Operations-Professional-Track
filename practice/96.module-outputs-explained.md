# Terraform 实操训练 96：模块 outputs 讲解版

## 1. 本节定位

本节主题是 **模块 outputs**。

在 Terraform 里，module 不是简单的“代码文件夹”，而是一个有输入、有资源、有输出的基础设施组件。  
`variable` 负责把参数传进模块，`output` 负责把模块内部计算出来的结果暴露给调用方。

这一节的重点不是背 `output` 语法，而是理解：

- 子模块为什么需要输出值；
- 调用方如何通过 `module.<模块名>.<output名>` 读取模块结果；
- output 是模块边界的一部分；
- output 会影响模块复用、测试断言、跨模块组合和 CI/CD 验证；
- 哪些 output 应该暴露，哪些不应该暴露。

---

## 2. 为什么模块需要 outputs

没有 output 的模块，像一个“黑盒施工队”：

- 你把参数交给它；
- 它创建了一些资源；
- 但它不告诉你资源最后叫什么、ID 是什么、生成了什么结果。

这在简单实验里还行，但在真实工程里很难继续组合。

例如一个网络模块创建了 VPC、subnet、security group。  
后面的计算模块、数据库模块、Kubernetes 模块都需要这些结果。  
这时就必须通过 output 把关键结果暴露出来。

典型用法是：

```hcl
module "network" {
  source = "./modules/network"

  name_prefix = "demo"
}

module "app" {
  source = "./modules/app"

  subnet_id          = module.network.subnet_id
  security_group_id = module.network.security_group_id
}
```

这里 `module.network.subnet_id` 和 `module.network.security_group_id` 就来自 network 模块里的 output。

---

## 3. output 的基本语法

模块内部可以这样声明输出：

```hcl
output "resource_name" {
  description = "The generated resource name"
  value       = local.resource_name
}
```

常见字段有：

```hcl
output "example" {
  description = "Explain what this output is for"
  value       = some_resource.example.id
  sensitive   = false
}
```

### 字段解释

| 字段 | 作用 |
|---|---|
| `output` 名称 | 调用方访问时使用的名字 |
| `description` | 给人看的说明，建议生产模块必须写 |
| `value` | 真正输出的值，可以来自 resource、local、variable、表达式 |
| `sensitive` | 是否隐藏敏感输出，避免在 CLI 中直接展示 |

---

## 4. 根模块 output 和子模块 output 的区别

Terraform 中有两种常见 output 场景。

### 4.1 根模块 output

根模块就是你执行 `terraform plan/apply/output` 的目录。

根模块中的 output 会显示在 CLI 输出里：

```hcl
output "app_name" {
  value = local.app_name
}
```

执行：

```sh
terraform output
```

会看到类似：

```text
app_name = "demo-app"
```

根模块 output 常用于：

- 给人看部署结果；
- 给 CI/CD 后续步骤读取；
- 给其他工具读取；
- 作为实验验证入口。

---

### 4.2 子模块 output

子模块 output 不会直接显示给 CLI。  
它主要是给调用方模块使用。

例如子模块中：

```hcl
output "name" {
  value = local.name
}
```

调用方中：

```hcl
output "module_name" {
  value = module.demo.name
}
```

注意：  
如果你希望 `terraform output` 看到子模块结果，需要在根模块里再转发一次。

也就是：

```hcl
output "demo_name" {
  value = module.demo.name
}
```

这个动作可以理解为：  
**子模块输出给根模块，根模块再输出给最终使用者。**

---

## 5. 本实验要观察什么

根据原始 lab 说明，本实验位于：

```sh
cd /workspace/practice/labs/96
```

核心流程是：

```sh
terraform init -input=false
terraform fmt -check
terraform validate
terraform test
terraform plan -input=false -no-color -out=tfplan
terraform apply -auto-approve tfplan
terraform output
terraform destroy -auto-approve
```

这些命令对应的学习目的如下。

| 命令 | 观察点 |
|---|---|
| `terraform init` | 模块路径、provider、依赖是否能初始化 |
| `terraform fmt -check` | HCL 格式是否符合标准 |
| `terraform validate` | output 引用是否有效，模块调用是否正确 |
| `terraform test` | output 是否满足测试断言 |
| `terraform plan` | output 是否会出现在计划结果中 |
| `terraform apply` | apply 后 output 是否有最终值 |
| `terraform output` | 根模块最终暴露了哪些结果 |
| `terraform destroy` | 清理资源，避免状态污染 |

---

## 6. 重点理解：output 是模块 API

Terraform module 的边界可以类比成函数：

```text
variable  = 函数参数
resource  = 函数内部逻辑
output    = 函数返回值
```

一个模块能不能被团队长期复用，很大程度取决于它的 API 设计。

### 好的 output 设计

好的 output 应该满足：

- 名字稳定；
- 含义清晰；
- 不暴露太多内部实现；
- 方便调用方组合；
- 有 description；
- 敏感值使用 `sensitive = true`；
- 不随便删除或改名。

例如：

```hcl
output "instance_id" {
  description = "ID of the created compute instance"
  value       = local.instance_id
}
```

### 差的 output 设计

不推荐这样：

```hcl
output "x" {
  value = local.name
}
```

问题是：

- 名字没有语义；
- 调用方不知道这个值能干什么；
- 后续维护者容易误用；
- 很难写清楚测试断言。

---

## 7. output 和测试断言的关系

这个阶段的 lab 已经开始和 `terraform test` 结合。  
测试断言经常会验证 output 是否符合预期。

例如：

```hcl
run "check_module_output" {
  command = plan

  assert {
    condition     = output.module_name == "demo-module"
    error_message = "module_name output should match expected value."
  }
}
```

这里的关键点是：

- 测试不是只看 Terraform 能不能跑；
- 测试还要验证模块暴露出来的结果是否符合预期；
- output 是测试模块行为最方便的入口之一。

也就是说，output 不只是给人看的，也是给自动化测试看的。

---

## 8. output 在生产环境里的价值

### 8.1 跨模块组合

最常见场景：

```hcl
module "network" {
  source = "./modules/network"
}

module "compute" {
  source = "./modules/compute"

  subnet_id = module.network.subnet_id
}
```

network 模块创建网络，compute 模块使用网络。  
中间连接点就是 output。

---

### 8.2 CI/CD 读取部署结果

CI/CD pipeline 里可以读取 output：

```sh
terraform output -json
```

然后把结果传给后续步骤，比如：

- 部署应用；
- 配置 DNS；
- 生成环境变量；
- 写入部署报告；
- 触发 smoke test。

更适合自动化的是：

```sh
terraform output -json > tf-output.json
```

因为 JSON 比纯文本稳定，适合脚本处理。

---

### 8.3 平台工程中的模块契约

平台团队写模块时，output 就是平台对应用团队承诺的结果。

例如：

```hcl
output "cluster_name" {
  description = "AKS/EKS cluster name used by application teams"
  value       = local.cluster_name
}
```

应用团队只需要使用这个 output，不需要知道模块内部到底创建了多少 resource。  
这就是模块封装的价值。

---

## 9. 常见错误

### 9.1 子模块没有 output，调用方却引用了它

错误示例：

```hcl
value = module.demo.name
```

但子模块中没有：

```hcl
output "name" {
  value = ...
}
```

会导致 validate 或 plan 失败。

修复方式：  
在子模块中补充对应 output。

---

### 9.2 output 名称改了，调用方没改

例如子模块从：

```hcl
output "name" {
  value = local.name
}
```

改成：

```hcl
output "resource_name" {
  value = local.name
}
```

调用方仍然写：

```hcl
module.demo.name
```

就会失败。

生产环境里，output 改名属于 breaking change。  
这类变化要像 API 变更一样谨慎。

---

### 9.3 输出了敏感信息

不要直接输出密码、token、密钥。

不推荐：

```hcl
output "admin_password" {
  value = random_password.admin.result
}
```

至少应该：

```hcl
output "admin_password" {
  value     = random_password.admin.result
  sensitive = true
}
```

但更好的方式是：  
敏感信息写入 Secret Manager、Key Vault、Vault 等系统，不要依赖 Terraform output 作为长期交付方式。

---

### 9.4 output 暴露过多内部实现

例如一个模块内部有很多临时 local 或中间资源 ID，不应该全部 output 出去。  
暴露越多，调用方耦合越深，未来模块越难重构。

模块 output 应该暴露“使用者真正需要的结果”，不是“内部所有细节”。

---

## 10. 建议的模块 output 设计规范

生产模块里建议遵守这些规则：

1. output 名称使用清晰的业务语义；
2. 每个 output 写 `description`；
3. 只暴露调用方确实需要的值；
4. 不输出敏感值，或者明确 `sensitive = true`；
5. 不随便删除、改名 output；
6. 对关键 output 写 `terraform test` 断言；
7. 对复杂 object output 写清楚结构；
8. CI/CD 优先使用 `terraform output -json`。

---

## 11. 和前面模块课程的关系

本节和前面几节是连续的：

| Lab | 主题 | 关系 |
|---|---|---|
| 88 | 创建自定义模块结构 | 开始把代码拆成模块 |
| 89 | 创建第一个本地模块 | 有了可调用的 module |
| 90 | module source | 学会模块来源写法 |
| 91 | 本地路径引用模块 | 学会从本地目录调用模块 |
| 92 | 改进模块硬编码 | 把硬编码变成可配置设计 |
| 93 | 模块变量 | 输入参数进入模块 |
| 94 | 调用方覆盖模块变量 | 调用方控制模块行为 |
| 95 | 模块 required_providers | 模块声明 provider 依赖 |
| 96 | 模块 outputs | 模块把结果返回给调用方 |

这一节可以理解为模块基础闭环的最后一块：

```text
source 找到模块
variable 输入参数
required_providers 声明依赖
resource 创建对象
output 返回结果
test 验证行为
```

---

## 12. 学完本节你应该能回答的问题

完成本 lab 后，你应该能回答：

1. 子模块里的 output 和根模块里的 output 有什么区别？
2. 为什么调用方要通过 `module.<name>.<output>` 读取子模块结果？
3. 为什么 output 是模块 API 的一部分？
4. 改 output 名字为什么可能是 breaking change？
5. 为什么敏感值不能随便 output？
6. `terraform output` 和 `terraform output -json` 分别适合什么场景？
7. `terraform test` 为什么经常验证 output？

---

## 13. 推荐复盘方式

跑完实验后，不要只看 `1 passed, 0 failed`。  
建议你重点看三个地方：

```sh
cat main.tf
find . -maxdepth 3 -type f | sort
terraform output
```

然后回答：

- 模块在哪里定义？
- 调用方怎么调用模块？
- 子模块暴露了哪些 output？
- 根模块有没有转发 output？
- 测试断言检查了哪个 output？
- 如果删除某个 output，会在哪里报错？

能回答这些，说明你不是“跑通了命令”，而是真的理解了 module output。

---

## 14. 本节结论

`output` 是 Terraform module 的返回值，也是模块对外的契约。

在真实工程里，output 的作用远远不只是“把结果打印出来”。  
它连接模块、支撑 CI/CD、服务自动化测试，也是平台团队设计可复用基础设施组件时必须认真管理的接口。

一句话总结：

> variable 决定模块怎么被配置，output 决定模块怎么被复用。
