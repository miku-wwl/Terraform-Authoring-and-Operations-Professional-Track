# 第 111 节任务

## 背景

AWS Provider Default Tags

## 要求

1. 在 provider 中配置 default_tags。
2. 创建一个继承默认标签的资源。
3. 创建一个覆盖 Team 标签的资源。

## 限制

- 不要使用真实 AWS。
- 不要把真实 access key 写入文件。
- 不要修改 `practice/labs/111/`。
