variable "service_name" {
  type        = string
  description = "Placeholder variable kept only so the starter configuration can initialize."
}

variable "environment" {
  type        = string
  description = "Placeholder variable kept only so the starter configuration can initialize."
}

variable "owner" {
  type        = string
  description = "Placeholder variable kept only so the starter configuration can initialize."
}
