# 第 153 节任务：挑战一概览：修复代码、导入、输出与状态管理

## 背景

本题来自第 153 节课程内容，属于综合挑战的理论/准备阶段。目标不是创建 AWS 资源，而是明确后续挑战的解题方法、题型范围和练习节奏。

## 要求

- 识别挑战一覆盖的修复、导入、输出和状态管理主题。
- 理解长题需要拆成小步骤验证。
- 为后续 154~156 的实操做准备。

## 验收

完成后执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\bootstrap.ps1
powershell -ExecutionPolicy Bypass -File scripts\verify.ps1
powershell -ExecutionPolicy Bypass -File scripts\clean.ps1
```

## 限制

- 不要启动真实 AWS 资源。
- 不要把真实 access key 写入文件。
- 不要修改 `practice/labs/153/`。
