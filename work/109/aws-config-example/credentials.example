# AWS CLI credentials 示例文件。
# 真实项目不要提交真正的 credentials。
# 本 lab 只使用 LocalStack 的 test/test。

[lab]
aws_access_key_id = test
aws_secret_access_key = test

[audit]
aws_access_key_id = test
aws_secret_access_key = test

