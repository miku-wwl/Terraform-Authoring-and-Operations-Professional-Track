# TODO 1: Read the current AWS caller identity from the configured provider.
# Hint: use data "aws_caller_identity" "current" {}.

# TODO 2: Read the current AWS provider region.
# Hint: use data "aws_region" "current" {}.
